{
  "name": "@foreningsregister/adapters",
  "version": "1.0.0",
  "description": "Adapters för olika kommuners föreningsregister API:er",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@foreningsregister/types": "workspace:*",
    "zod": "^3.22.0",
    "openapi-fetch": "^0.9.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.3.0"
  }
}
